# Ruff linter config
line-length = 120
target-version = "py311"

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # Allow long lines in some cases

[lint.per-file-ignores]
"tests/*" = ["E501"]
"app.py" = ["E501"]   # CSS strings are long

[format]
quote-style = "double"
